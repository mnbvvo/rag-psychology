# Web 框架
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# LLM / RAG（LangChain 生态）
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0
langchain-text-splitters>=0.2.0
chromadb>=0.5.0

# 文档加载（PDF / DOCX；当前主链路走 JSONL，按需启用）
pypdf>=4.0.0
docx2txt>=0.9.0

# 说明：
# - sentence-transformers / unstructured 此前未实际使用，已移除。
#   若日后要用本地嵌入或 UnstructuredMarkdownLoader，再单独补装。
# - 推荐锁定版本到本项目实测通过的 release，例如用 pip freeze > requirements.lock。
