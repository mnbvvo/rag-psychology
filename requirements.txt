# Web 框架
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# LLM / RAG（LangChain 生态，仅保留主链路实际 import 的包）
langchain-openai>=0.1.0
langchain-chroma>=0.1.0
langchain-core>=0.2.0
chromadb>=0.5.0

# 说明：
# - langchain / langchain-community / langchain-text-splitters / pypdf / docx2txt
#   此前未实际使用，或仅被已删除的 modules/document_processor.py 使用，故移除。
#   若日后要扩展 PDF/Markdown 导入再单独补装对应依赖。
# - 推荐锁定版本到本项目实测通过的 release，例如用 pip freeze > requirements.lock。
