# Web 框架
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# LLM / RAG（LangChain 生态，仅保留主链路实际 import 的包）
langchain-openai>=0.1.0
langchain-chroma>=0.1.0
langchain-core>=0.2.0
chromadb>=0.5.0

# 关系型数据库（结构化持久化：会话 / 消息 / 危机审计，与 Chroma 向量库互补）
sqlalchemy>=2.0.0

# LLM 客户端（langchain-openai 的运行时依赖，对话实际走它；不显式列出会依赖传递安装，环境不可复现）
openai>=1.30.0
# HTTP 客户端（Starlette / langchain 底层使用）
httpx>=0.27.0

# 本地重排序（Cross-Encoder，bge-reranker-v2-m3）
# torch 依赖较大且分 CPU/CUDA 版：CPU 直接 pip install torch；
# 有 NVIDIA GPU 建议装 CUDA 版（python -m pip install torch --index-url https://download.pytorch.org/whl/cu124）
sentence-transformers>=3.0.0

